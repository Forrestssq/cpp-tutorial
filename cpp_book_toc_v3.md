# 《现代 C++：从 C 出发》目录 v3

主线项目：Taco 脚本语言解释器（文件后缀 .taco）

---

## 前言
- 这本书是为谁写的
- 如何阅读这本书（螺旋式结构说明）
- 主线项目介绍：我们要做什么
- 环境配置：编译器、编辑器、CMake

---

## 第一部分：迈入 C++

**第 1 章：C++ 是什么**
- 1.1 从 C 到 C++：不只是"加了类的 C"
- 1.2 C++ 的设计哲学：零开销抽象
- 1.3 现代 C++ 与古老 C++ 的区别
- 1.4 第一个程序：Hello, World

**第 2 章：构建系统与第三方库**
- 2.1 为什么需要构建系统
- 2.2 CMake 基础：最小可用配置
- 2.3 引入第三方库：FetchContent 与 vcpkg
- 2.4 静态库与动态库的区别
- 2.5 头文件与链接：底层发生了什么

**第 3 章：基础语法升级**
- 3.1 `auto` 与类型推导
- 3.2 引用：比指针更安全的别名
- 3.3 `const` 的正确用法
- 3.4 函数重载与默认参数
- 3.5 `namespace` 与作用域

**第 4 章：字符串与输入输出**
- 4.1 `std::string` vs C 字符串
- 4.2 `iostream`：`cin` 与 `cout`
- 4.3 格式化输出：`<format>`（C++20）与 fmtlib
- 4.4 文件 I/O：`<fstream>`
- 4.5 `<filesystem>`：现代文件系统操作

**第 5 章：解释器是什么**
- 5.1 语言、编译器、解释器的关系
- 5.2 Taco 语言设计：我们要实现什么
- 5.3 解释器的三层架构：词法、语法、求值
- 5.4 整本书的项目路线图

**第 6 章：项目 v0——词法分析器**
- 6.1 什么是 Token
- 6.2 用结构体和函数实现基础词法分析器
- 6.3 能识别：数字、字符串、运算符、关键字
- 6.4 测试：把 `var x = 10;` 切成 Token 列表
- 6.5 这个版本的局限性

---

## 第二部分：类与对象

**第 7 章：类的基础**
- 7.1 从 Python 的类到 C++ 的类：对比切入
- 7.2 成员变量与成员函数
- 7.3 访问控制：`public`、`private`、`protected`
- 7.4 `this` 指针

**第 8 章：构造与析构**
- 8.1 构造函数：对象如何诞生
- 8.2 析构函数：对象如何消亡
- 8.3 初始化列表
- 8.4 RAII 初步：资源与对象生命周期绑定
- *More About 构造函数*：默认构造、`explicit`、委托构造

**第 9 章：拷贝与赋值**
- 9.1 拷贝构造函数
- 9.2 拷贝赋值运算符
- 9.3 Rule of Three
- 9.4 为什么拷贝有时候很危险
- *More About 拷贝*：深拷贝 vs 浅拷贝的底层图景

**第 10 章：运算符重载**
- 10.1 什么是运算符重载
- 10.2 常用运算符重载：`+`、`==`、`<<`
- 10.3 什么时候该重载，什么时候不该

**第 11 章：项目 v1——语法分析器与 AST**
- 11.1 什么是抽象语法树（AST）
- 11.2 用类表示 AST 节点：`Expr`、`Stmt`
- 11.3 实现递归下降解析器
- 11.4 能解析：变量声明、基本表达式
- 11.5 测试：把 `var x = 10 + 20;` 解析成 AST
- 11.6 这个版本的局限性

---

## 第三部分：继承与多态

**第 12 章：继承**
- 12.1 继承的概念与语法
- 12.2 与 Python 继承的异同
- 12.3 构造函数的继承与调用顺序
- 12.4 `protected` 的真实含义
- *More About 继承*：多重继承与菱形问题、虚继承

**第 13 章：多态与虚函数**
- 13.1 为什么需要多态
- 13.2 虚函数：运行时决策
- 13.3 纯虚函数与抽象类
- 13.4 `override` 与 `final`
- *More About 虚函数*：vtable 的底层实现

**第 14 章：类型转换**
- 14.1 隐式转换的风险
- 14.2 `static_cast`、`dynamic_cast`、`reinterpret_cast`、`const_cast`
- 14.3 `dynamic_cast` 与运行时类型识别（RTTI）

**第 15 章：项目 v2——求值器与控制流**
- 15.1 用继承设计 AST 节点体系
- 15.2 `Expr` 基类，`BinaryExpr`、`IfExpr`、`WhileExpr`、`ForExpr` 子类
- 15.3 用虚函数实现 `evaluate()`
- 15.4 能运行：if/elseif/else、while、for、switch
- 15.5 字符串插值的实现
- 15.6 测试：运行第一个真实的 Taco 程序
- 15.7 这个版本的局限性

---

## 第四部分：现代 C++ 核心

**第 16 章：所有权与生命周期**
- 16.1 C++ 的内存模型：栈与堆
- 16.2 裸指针的问题
- 16.3 所有权是什么
- 16.4 RAII 深讲

**第 17 章：智能指针**
- 17.1 `unique_ptr`：独占所有权
- 17.2 `shared_ptr`：共享所有权
- 17.3 `weak_ptr`：打破循环引用
- 17.4 什么时候用哪个
- *More About 智能指针*：引用计数的底层实现

**第 18 章：移动语义**
- 18.1 左值与右值
- 18.2 移动构造函数与移动赋值运算符
- 18.3 `std::move` 的本质
- 18.4 Rule of Five
- *More About 移动语义*：完美转发与 `std::forward`

**第 19 章：常用标准库补充**
- 19.1 `<chrono>`：时间与性能测量
- 19.2 `<optional>`、`<variant>`、`<any>`
- 19.3 `<random>`：随机数
- 19.4 `<regex>`：正则表达式

**第 20 章：项目 v3——函数、闭包与环境**
- 20.1 用智能指针管理 AST 节点
- 20.2 环境（Environment）：作用域的实现
- 20.3 用 RAII 管理函数调用栈
- 20.4 实现闭包：捕获外部变量
- 20.5 能运行：函数定义、调用、递归、闭包、多返回值、命名参数
- 20.6 测试：斐波那契数列、闭包捕获变量
- 20.7 这个版本的局限性

---

## 第五部分：STL

**第 21 章：容器**
- 21.1 `vector`：最常用的序列容器
- 21.2 `deque`、`list`：其他序列容器
- 21.3 `map` 与 `unordered_map`
- 21.4 `set` 与 `unordered_set`
- 21.5 `stack`、`queue`、`priority_queue`
- 21.6 如何选择容器
- *More About 容器*：底层数据结构与复杂度分析

**第 22 章：迭代器**
- 22.1 迭代器是什么
- 22.2 迭代器的种类
- 22.3 范围 for 循环的本质
- *More About 迭代器*：迭代器设计哲学与 ranges（C++20 预览）

**第 23 章：算法库**
- 23.1 不修改序列：`find`、`count`、`all_of`、`any_of`
- 23.2 修改序列：`transform`、`copy_if`、`replace`、`remove`
- 23.3 排序相关：`sort`、`stable_sort`、`partial_sort`、`nth_element`
- 23.4 数值算法：`accumulate`、`reduce`、`inner_product`
- 23.5 其他常用：`rotate`、`partition`、`unique`、`merge`
- 23.6 避免手写循环的思维方式

**第 24 章：Lambda 表达式**
- 24.1 Lambda 的语法
- 24.2 捕获列表：值捕获与引用捕获
- 24.3 Lambda 与算法的配合
- 24.4 `std::function`
- *More About Lambda*：Lambda 的底层——函数对象

**第 25 章：项目 v4——array、map、enum 与标准库**
- 25.1 用 STL 容器实现 Taco 的 array 和 map
- 25.2 实现 enum 与关联值
- 25.3 实现 pipeline：`filter`、`map`、`each`、`reduce`
- 25.4 实现 Swift 风格闭包语法：`{ x in ... }`
- 25.5 实现可选链 `?.` 与默认值 `??`
- 25.6 实现展开运算符 `...`
- 25.7 内置标准库：string、array、map、文件、系统方法
- 25.8 测试：运行完整的 Taco 脚本
- 25.9 这个版本的局限性

---

## 第六部分：模板

**第 26 章：函数模板**
- 26.1 为什么需要模板
- 26.2 函数模板的写法
- 26.3 模板类型推导
- 26.4 模板与重载的关系

**第 27 章：类模板**
- 27.1 类模板的写法
- 27.2 模板参数的默认值
- 27.3 成员函数模板
- 27.4 模板与继承

**第 28 章：模板进阶**
- 28.1 模板特化：全特化与偏特化
- 28.2 可变参数模板
- 28.3 `if constexpr`：编译期条件
- 28.4 编译期计算：`constexpr`
- *More About 模板*：SFINAE 初步与 `concepts`（C++20）

**第 29 章：项目 v5——解释器内部模板化**
- 29.1 用 `std::variant` 实现 Taco 值类型
- 29.2 用模板重构符号表与环境
- 29.3 用模板实现通用的 AST 节点访问器
- 29.4 模板在这里解决了什么问题
- 29.5 性能对比：重构前后的差异

---

## 第七部分：多线程

**第 30 章：并发基础**
- 30.1 进程与线程的区别
- 30.2 `std::thread`：创建和管理线程
- 30.3 线程的生命周期：`join` 与 `detach`
- 30.4 竞态条件：什么时候出问题

**第 31 章：同步原语**
- 31.1 `std::mutex` 与 `std::lock_guard`
- 31.2 `std::unique_lock`：更灵活的锁
- 31.3 `std::condition_variable`：线程间通信
- 31.4 死锁：如何产生，如何避免

**第 32 章：原子操作与异步**
- 32.1 `std::atomic`：无锁编程入门
- 32.2 `std::future` 与 `std::promise`
- 32.3 `std::async`：简单的异步任务
- *More About 多线程*：内存模型与内存序

**第 33 章：项目 v6——REPL 与并发安全**
- 33.1 什么是 REPL
- 33.2 立刻显示表达式结果
- 33.3 多行输入的处理
- 33.4 历史记录：上下箭头
- 33.5 用线程分离输入与求值
- 33.6 用 channel 实现线程间通信
- 33.7 保护解释器的共享状态
- 33.8 清晰的错误提示，不崩溃
- 33.9 让 REPL 变成游乐场：细节打磨

---

## 第八部分：网络编程初步

**第 34 章：网络编程基础**
- 34.1 TCP/UDP、客户端/服务器模型
- 34.2 用原生 socket API 写一个最简单的例子
- 34.3 同步 I/O vs 异步 I/O 的概念

**第 35 章：现代 C++ 网络库**
- 35.1 Asio（独立版）：现代 C++ 风格的网络库
- 35.2 用 Asio 写一个 TCP 客户端
- 35.3 `cpp-httplib`：几行代码发一个 HTTP 请求
- 35.4 网络生态概览：什么场景用什么库

**第 36 章：项目 v7——内置 fetchUrl() 与网络支持**
- 36.1 在 Taco 里实现 `fetchUrl()`
- 36.2 用 cpp-httplib 发 HTTP 请求
- 36.3 解析 JSON 响应：nlohmann/json
- 36.4 实现 `postData()`
- 36.5 测试：用 Taco 脚本调用真实 API

---

## 第九部分：综合收尾

**第 37 章：Taco 完整版**
- 37.1 回顾七次进化
- 37.2 最终版本的完整架构图
- 37.3 模块系统的实现：`import`
- 37.4 还能怎么扩展：垃圾回收、字节码、标准库

**第 38 章：VSCode 插件与 Language Server**
- 38.1 LSP 是什么
- 38.2 语法高亮：TextMate grammar
- 38.3 实现基础 Language Server
- 38.4 自动补全与错误提示
- 38.5 发布到 VSCode Marketplace

**第 39 章：C++ 生态与工程实践**
- 39.1 常用第三方库概览：nlohmann/json、spdlog、Catch2
- 39.2 测试：用 Catch2 给 Taco 写单元测试
- 39.3 调试工具：gdb、valgrind、AddressSanitizer
- 39.4 代码风格：clang-format、clang-tidy
- 39.5 发布到 GitHub：README、CI、二进制发布

**第 40 章：接下来去哪里**
- 40.1 没讲到的重要内容：异常处理、协程（C++20）、模块（C++20）
- 40.2 Taco 的下一步：垃圾回收、字节码虚拟机、JIT
- 40.3 网络编程深入：Boost.Asio、gRPC
- 40.4 值得读的书与资源
- 40.5 C++ 的未来：C++23 及之后

---

## 附录

**附录 A：Taco 语言完整设计文档**
**附录 B：Taco 标准库参考**
**附录 C：C++ 关键字速查**
**附录 D：STL 容器与算法速查**
